package com.NetworkApp.model;

import com.NetworkApp.entities.User;

import java.util.Map;

public class ModelUserTable {
    private Map<Integer, User> model;
    /*public void getData(User user) {
        model.put(user.getUserId(), user);
    }*/
}